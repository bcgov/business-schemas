{
    "definitions": {},
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://bcrs.gov.bc.ca/.well_known/schemas/court_order",
    "type": "object",
    "title": "Court Order Information Schema",
    "properties": {
        "fileNumber": {
            "type": "string",
            "minLength": 5,
            "maxLength": 20,
            "description": "The court assigns each court order a unique file number up to 20 characters in length."
        },
        "orderDate": {
            "type": "string",
            "description": "The date of the court order in the ISO 8601 format YYYY-MM-DDThh:mm:ssTZD.",
            "format": "date-time"
        },
        "effectOfOrder": {
            "type": "string",
            "minLength": 5,
            "maxLength": 500,
            "description": "A brief note to explain the purpose of the Court Order."
        },
        "hasPlanOfArrangement": {
            "type": "boolean",
            "description": "Is this filing pursuant to a Plan of Arrangement?"
        }
    },
    "required": [
        "fileNumber",
        "orderDate"
    ]
}
